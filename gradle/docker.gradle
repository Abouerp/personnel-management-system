jib {
    from {
        image = 'openjdk:12'
    }
    to {
        image = "abouerp.top/${project.name}"
        tags = ['latest']
    }
    container {
        labels = ['maintainer': 'abouerp "me@abouerp.top"']
    }
}
